# New Repo

This directory is a starter scaffold for a new repository.

## Included
- `README.md` (this file)
- `.gitignore` with common defaults
- `LICENSE` (MIT)

## To use this as an actual standalone repository
1. Copy this directory out of the current project.
2. Run `git init` in the copied directory.
3. Create a new remote repository and push.
